# .env.example
# MCP server base URL (default points to the public Verden MCP endpoint)
MCP_BASE_URL=https://verden2.lovable.app/mcp

# Optional custom mapping file path (relative to project root)
#MCP_MAPPING_PATH=./mcp-mapping.json

# Port on which the wrapper will listen (default 3000)
PORT=3000
